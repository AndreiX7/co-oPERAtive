#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/ruby" ]; then
  "$basedir/ruby"  "$basedir/../nem/bin/nem" "$@"
  ret=$?
else 
  ruby  "$basedir/../nem/bin/nem" "$@"
  ret=$?
fi
exit $ret
